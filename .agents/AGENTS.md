# Project Rules

- **Holistic Deployment Awareness**: Never leave out any single detail when planning or executing project deployments. Always verify and explicitly remind the user to configure production environment variables (e.g., API keys, wallet addresses) *before* triggering deployments or pushing code to deployment branches (like Render, Vercel, etc).
